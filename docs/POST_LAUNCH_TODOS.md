# Post-launch TODOs

Items called out during Wave 7 that can ship after launch without
blocking it.

## Pricing

- **Placeholder prices in `api/app/routes/billing.py:PLAN_PRICES_CENTS` are
  defensible defaults, not final — pricing team to confirm post-launch.**
  Current placeholders: `free $0 / pro $5 / ultra $15 / max $50` per month.
  The quota ladder (5 GB / 100 GB / 1 TB / 5 TB) is locked; only the
  price cents need sign-off.
